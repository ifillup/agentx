const Transaction = ({ transaction }) => {
  return <h5>{Object.values(transaction)}</h5>;
};

export default Transaction;

{
  /* club: '',
    playername: '',
    playerID: '',
    profit: '',
    rake: '', */
}
